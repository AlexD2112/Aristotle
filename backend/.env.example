# backend/.env.example
# Copy to backend/.env and fill in secrets or local values (do NOT commit backend/.env)

# AWS (leave blank to use IAM role or AWS CLI config)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# Flask
FLASK_ENV=development
FLASK_HOST=0.0.0.0
FLASK_PORT=6767
